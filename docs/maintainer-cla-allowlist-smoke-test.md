# Maintainer CLA Allowlist Smoke Test

This temporary document verifies that the current maintainer account can open a pull request and pass the CLA workflow through the repository collaborator allowlist.

The file should not be merged into `main`.
